<UserControl x:Class="Gulayan.Controls.Legends"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mashappsMateriaDesignIcons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:views="clr-namespace:Gulayan.Views"
             xmlns:viewModels="clr-namespace:Gulayan.ViewModels"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    
             d:Background="White"
             FontFamily="{StaticResource Ubuntu}"
             mc:Ignorable="d">
 

    <StackPanel Orientation="Horizontal">
        <Border Background="{StaticResource SecondaryColor}" 
                CornerRadius="3" 
                Width="12"
                Height="12"/>
        <TextBlock Foreground="#6c879a"
                   Margin="7 0 20 0" 
                   Text="Marketing" 
                   VerticalAlignment="Center"/>

        <Border Background="{StaticResource PrimaryColor}" 
                CornerRadius="3" 
                Width="12"
                Height="12"/>
        <TextBlock Foreground="#6c879a"
                   Margin="7 0 20 0" 
                   Text="Sales" 
                   VerticalAlignment="Center"/>
    </StackPanel>
</UserControl>